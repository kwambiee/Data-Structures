// An array is a type of data structure that stores elements of the same type in a contiguous block of memory.

function reverseArray(arr) {
  return arr.reverse();
}
console.log(reverseArray([4, 3, 2, 7]));
